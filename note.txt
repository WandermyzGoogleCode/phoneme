删除群内不存在的成员，远程同步去掉已经不在的组

群组搜索，申请加入群组

服务器配置文件

IdenticalInfoField的函数都实现一个id版本的

单机多用户

调用服务器方法时，加上有效性判断。有效性验证信息可以在登录时生成并给出。
并且，每做一次操作考虑更换一个验证信息，防止信息被截取，然后用来蒙骗服务器。

被授权联系人的统一化

各种DISABLE与ENABLE
